# PyTorch CPU 版本（云平台无 GPU，必须用 CPU 版）
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0,<3.0.0

# 核心依赖（宽松版本，减少冲突）
langchain>=0.3.0,<0.4
langchain-openai>=0.2.0
langgraph>=0.2.0

# 向量数据库
chromadb>=0.5.0,<0.6

# 文本向量化与重排序
sentence-transformers>=3.0.0
rank-bm25>=0.2.2

# 文档解析
PyMuPDF>=1.24.0
python-docx>=1.0.0

# 前端
streamlit>=1.33.0

# 工具
python-dotenv>=1.0.0
pydantic>=2.0.0
numpy>=1.26.0,<2.0
tqdm>=4.66.0

# 联网搜索
requests>=2.28.0
